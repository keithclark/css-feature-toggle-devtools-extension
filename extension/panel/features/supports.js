import {atRuleIdentifierOption} from '../feature-helpers.js';

export default atRuleIdentifierOption({
  name: 'Feature detection',
  group: 'Other',
  help: 'Disable `@supports` feature detection',
  identifier: 'supports'
});
