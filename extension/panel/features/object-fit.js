import {propertyNameOption} from '../feature-helpers.js';

export default propertyNameOption({
  name: 'Object sizing',
  group: 'Content Layout',
  help: 'Disable support for the `object-fit` property',
  propertyNames: [
    'object-fit'
  ]
});
